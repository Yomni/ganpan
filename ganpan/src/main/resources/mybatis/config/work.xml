<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper

PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"

"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- MyBatis에서 namespace는 필수적으로 명시해야 함 -->

 <mapper namespace="work">
   
	<!-- <resultMap id="workRM" type="workVO">
		<result property="workNo" column="work_no"/>
		<result property="workName" column="work_name"/>
		<result property="workDetails" column="work_details"/>
		<result property="creationDate" column="creation_DATE"/>
		<result property="changeDate" column="change_DATE"/>
		
		<result property="" column="board_no"/>
		<result property="" column="worker_nickname"/>
		<result property="" column="sign_board_name"/>
		<result property="" column="boss_nickname"/>
	</resultMap> -->
	
 	<!-- <select id="showContentList" resultMap="workRM" parameterType="signBoardVO">
 		select  work_no, work_name, worker_nickname, board_no, sign_board_name, boss_nickname
		from work
		where boss_nickname=#{haveBoardVO.signBoardVO.bossMemberVO.nickName} and sign_board_name=#{haveBoardVO.signBoardVO.signBoardName}
 	</select> -->
 	
</mapper>







